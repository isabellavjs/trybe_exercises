let numbers = [5, 9, 3, 19, 70, 8, 100, 2, 35, 27];

//Percorrer cada elemento do array, e imprimir os valores.
for (let indice = 0; indice < numbers.length; indice++) {
    console.log(numbers[indice])
}